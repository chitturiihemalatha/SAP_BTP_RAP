_schema-version: "3.2"
ID: travusthtravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travusthtravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travusthtravelproc-destination-service
    parameters:
      content-target: true
  - name: travusthtravelproc-repo-host
    parameters:
      service-key:
        name: travusthtravelproc-repo-host-key
  - name: travusthtravelproc-uaa
    parameters:
      service-key:
        name: travusthtravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travusthtravelproc_html_repo_host
          ServiceInstanceName: travusthtravelproc-html5-srv
          ServiceKeyName: travusthtravelproc-repo-host-key
          sap.cloud.service: travusthtravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travusthtravelproc_uaa
          ServiceInstanceName: travusthtravelproc-xsuaa-srv
          ServiceKeyName: travusthtravelproc-uaa-key
          sap.cloud.service: travusthtravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travusthtravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travusthtravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travusthtravelproc.zip
      name: travusthtravelproc
      target-path: resources/
- name: travusthtravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travusthtravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travusthtravelproc-destination-service
    service-plan: lite
- name: travusthtravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travusthtravelproc-xsuaa-srv
    service-plan: application
- name: travusthtravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travusthtravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
